# DS_LM_chart_explore
This branch (add-scale-png) positions a PNG of a scale to depict the colors for the representation ratio. 
Not ideal-- want to create a new branch that shws a legend and scale simutaneously via highcharts (which will require doing the coloring through high charts). Will need a whole new set up, maybe even a new repository
